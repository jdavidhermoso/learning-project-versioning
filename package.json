{
  "name": "versioning-project",
  "version": "1.0.0",
  "description": "Playing with commitizen and standard-version to learn how to version an app releases",
  "main": "index.js",
  "scripts": {
    "release": "standard-version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jdavidhermoso",
  "license": "ISC",
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0",
    "standard-version": "^4.4.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
